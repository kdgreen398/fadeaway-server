const AppointmentStatuses = Object.freeze({
  PENDING: "pending",
  ACCEPTED: "accepted",
  DECLINED: "declined",
  CANCELED: "canceled",
  COMPLETED: "completed",
});

module.exports = AppointmentStatuses;
