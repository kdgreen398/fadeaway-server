const FETCH_BARBERS_BY_CITY_STATE = `SELECT * FROM USERS WHERE CITY = ? AND STATE = ? AND ACCOUNT_TYPE = 'BARBER'`;
const FETCH_IMAGES_BY_BARBER_ID = `SELECT IMAGE_ID, NAME FROM BARBER_IMAGES WHERE USER_ID = ?`;
const FETCH_REVIEWS_BY_BARBER_ID = `SELECT REVIEWS.REVIEW_ID, REVIEWS.RATING, REVIEWS.DESCRIPTION, REVIEWS.DATE_CREATED, CONCAT(USERS.FIRST_NAME, ' ', USERS.LAST_NAME) as AUTHOR FROM REVIEWS INNER JOIN USERS ON USERS.USER_ID = REVIEWS.REVIEWER_ID WHERE BARBER_ID = ?`;

module.exports = {
  FETCH_BARBERS_BY_CITY_STATE,
  FETCH_IMAGES_BY_BARBER_ID,
  FETCH_REVIEWS_BY_BARBER_ID,
};
